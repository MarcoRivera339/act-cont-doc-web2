import { ApellidoClientePipe } from './apellido-cliente.pipe';

describe('ApellidoClientePipe', () => {
  it('create an instance', () => {
    const pipe = new ApellidoClientePipe();
    expect(pipe).toBeTruthy();
  });
});
