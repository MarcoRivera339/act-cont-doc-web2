import { NombreClientePipe } from './nombre-cliente.pipe';

describe('NombreClientePipe', () => {
  it('create an instance', () => {
    const pipe = new NombreClientePipe();
    expect(pipe).toBeTruthy();
  });
});
